# Codex Benchmark Workspace

Tracked fixture for the Codex CLI A/B benchmark.

- Storefront checkout app with a shared CTA package and a local wrapper component.
- Admin checkout app as a distractor surface for cross-app routing.
- Backend readiness controller and service with a real spec path.
- Playwright spec and package-level scripts that point at real tracked verification files.
